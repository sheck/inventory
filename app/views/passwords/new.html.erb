<div id="clearance" class="password-reset">
  <h2><%= t(".title") %></h2>

  <p><%= t(".description") %></p>

  <%= simple_form_for :password, url: passwords_path do |form| %>
    <%= form.input :email %>

    <%= form.button :submit %>
  <% end %>
</div>
